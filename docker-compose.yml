version: '3'
services:
    fairchess:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: fairchess
        env_file: .env
        environment:
            - NODE_ENV=$NODE_ENV
        image: fairchess
        networks:
            - app-network
        ports:
            - "3000:3000"
        restart: unless-stopped

networks:
    app-network:
        driver: bridge
